default:
  image: node:22-alpine

pages:
  stage: deploy
  script:
    - yarn install --frozen-lockfile
    - yarn build
    - mv dist public
  artifacts:
    paths:
      - public
  rules:
    - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH
